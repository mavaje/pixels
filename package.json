{
  "scripts": {
    "build": "npx tsc && npx esbuild main.js --bundle --format=esm --outfile=src.js"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "firebase": "^12.10.0"
  }
}
